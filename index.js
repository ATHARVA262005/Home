var typed = new Typed('#heroMsg', {
    strings: ['Step into a world where imagination knows no bounds', ' where colors and concepts collide','where creativity finds its truest expression'],
    typeSpeed: 50,
    backSpeed: 25,
    loop :true,
  });